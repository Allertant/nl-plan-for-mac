# NL Plan 测试环境变量配置
#
# 使用方法：
#   1. 复制本文件为 .env
#      cp .env.example .env
#   2. 将下面的占位符替换为你的真实 DeepSeek API Key
#   3. 运行测试（会自动加载 .env 文件）：
#      swift test --filter AIModelStability
#
# 注意：.env 文件已加入 .gitignore，不会被提交到仓库

NLPLAN_API_KEY=your_deepseek_api_key_here
