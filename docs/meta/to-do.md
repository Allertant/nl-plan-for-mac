存在的问题(完成后标记一下):

1. ~~输入框输入自己的想法后,AI 实际上没有筛选出已有的想法,导致加入到想法池中的想法重复了,这一点是否有交代给AI 进行说明呢?~~ ✅ 已解决：AI prompt 已包含去重指令，解析时传入已有想法标题。
2. ~~AI 分析想法判定是否为项目不是很准确,需要探讨一下如何让 AI 厘清普通想法和 AI 之间的关系.~~ ✅ 已解决：优化了 classifyProjects prompt，添加核心判断原则、正反示例和边界情况说明。
3. ~~AI 处理想法的待确认页面,追问 AI 时,面板上需要同步其状态,当前在追问状态下,面板未显示其状态信息.~~ ✅ 已解决：QueueDetailView 追问时显示 "AI 正在处理你的调整..." 状态条。
4. ~~输入想法提示词修正,不要加上与今日推荐相关的功能,现在 AI 返回展示的结果中有一个🌟AI 推荐标识的问题,这是不需要的,也不需要 AI 返回.~~ ✅ 已解决：prompt 移除 recommended/reason 字段，DTO 改为可选，审核页面移除 AI 推荐标识和评语展示。
5. ~~提示词更新,如果遇到"见抖音收藏","了解..."之类的字眼,说明这是了解性质的,不需要作为项目.~~ ✅ 已解决：classifyProjects prompt 添加排除规则，浏览性描述一律为普通想法。
6. ~~提示词更新,如果遇到仅一个网址或者平台或者了解一段历史的什么无其他说明的,仅作为普通想法,不适合作为长期项目.~~ ✅ 已解决：同上，添加了"明确排除规则"。
7. ~~提示词更新,再次界定项目,一定是一次性无法完成的,需要长时间持续投入的,例如电视剧,系列电影,如果是单个电影就不算是项目.~~ ✅ 已解决：添加了系列电影=项目、单部电影≠项目的示例和规则。
8. ~~在审核 AI 生成的想法页面,允许用户修改所属类别/想法类型/时间/添加或修改备注,修改方式参考想法池页面针对普通想法的修改,当前页面设计的自带的编辑图标已经过时,关于这个逻辑删除掉,~~ ✅ 已解决：ParsedTaskRow 支持编辑标题/分类/时长/备注，ParsedTask 增加 note 字段。
9. ~~提示词更新,审核界面,每个想法有一段类似于"可稍后了解,不紧急"之类的 AI 给出的评语,不需要,不用展示也不用存储.~~ ✅ 已解决：prompt 移除 reason 字段，ParsedTaskRow 移除评语展示。
10. ~~审核页面,我的输入是不可编辑的,这里允许用户进行编辑.~~ ✅ 已解决：原始输入区域增加编辑/保存按钮，支持修改文本。
11. ~~审核界面,允许用户单个单个 approve 加入,当然也保留页面末尾的批量全部加入,加入后直接入库,从当前页面删除,如果后续要跟 AI 进行对话,仅把剩余的部分发送给 AI,并告诉 AI 没有发的是用户已经审核通过了的,不用对齐进行处理了.~~ ✅ 已解决：每个 ParsedTaskRow 增加"通过"按钮，点击后单独入库并从列表移除；保留"全部确认添加"。
12. ~~审核页面头部缺少 TITILE 名称, 可以命名为"想法审核"~~ ✅ 已解决：QueueDetailView 顶部栏居中显示"想法审核"标题。
13. ~~历史详情页面点击查看项目,进入到项目详情页,可以直接使用想法池进入到项目详情页的那个代码,一模一样,可以抽离这部分代码形成一个单独的组件,两处分别调用,保证逻辑的一致性.~~ ✅ 已解决：创建 ProjectNavLink 统一组件，历史详情使用该组件导航，想法池通过行点击复用同一 ProjectDetailContainerView。
14. ~~想法池页面到代码过于庞大,考虑进行组件化拆分.~~ ✅ 已解决：拆分为 IdeaPoolSection（搜索+列表）、IdeaPoolTaskRow（卡片行）、IdeaPoolSearchHelpers（搜索标签组件/键盘监听/刷新图标）三个文件。
15. ~~探究为什么 "macbook neo 深入了解, 并形成一篇推文." 为什么被判定为项目, 由此更新提示词.~~ ✅ 已解决：classifyProjects 排除规则增加"了解 X 并形成推文/笔记/文章"和"深入了解 X + 单次可交付动作"均为普通想法。
16. ~~审核页面, 当前仅有一个项目,我输入提示词让 AI 将其变为一个普通想法,但是返回后页面仍旧是一个项目, AI 并没有对其改变,这是因为 AI 没有理解还是提示词有问题还是本地更新逻辑有问题.~~ ✅ 已解决：追问后通过标题匹配迁移用户已设置的 isProject 值，仅对 isProject=nil 的新任务调用 AI 分类。
17. ~~AI 推荐是否会读取想法池中的备注信息?~~ ✅ 已解决：TaskRecommendationInput 增加 note 字段，推荐提示词中展示备注内容。
18. ~~AI 推荐提示词更新: 如果一个想法或项目是类似于参加会议之类的,那么可以重点参考其备注或截止时间,若没有特殊说明说要提前准备的, 一般当天再推荐,毕竟会议当天才会举办, 不要提前推荐.~~ ✅ 已解决：推荐提示词增加会议/事件类当天推荐规则。
19. ~~AI 推荐时是否有将必做项的完成状态和当前计时进度加上,让 AI 能精确地计算剩余时间.~~ ✅ 已解决：mustDoInputs 构建时拼入已用时间，推荐提示词排除已完成任务并扣除已用时间。
20. ~~如果 AI 推荐项来自于项目,那么参考必做项的链接按钮,加上这个链接按钮,点击后和那个按钮一样显示链接的项目.~~ ✅ 已解决：RecommendationRow 添加 ideaPoolIdeas 参数，来源为项目时显示链接按钮。
21. ~~AI 推荐结果中,标题和备注过长的都被截断了,不如多行展示完全,这样至少让用户明白全貌~~ ✅ 已解决：标题和推荐理由使用 fixedSize + 移除 lineLimit，多行完整展示。
22. ~~驳斥评分没有加载动画.~~ ✅ 已解决：驳斥提交后显示 ProgressView + "AI 正在重新评分..."。
23. ~~验证必做项计时功能是否存在 bug,交付给今日总结是否有 bug,导致部分必做项的时间被吞掉.~~ ✅ 已解决：markComplete 对运行中任务补上 endSession，防止最后一轮运行时间丢失。
24. ~~项目详情标题无法编辑, 参考想法池页面普通想法的编辑模式实现一下.~~ ✅ 已解决：项目详情标题支持点击编辑，失焦或回车提交保存。
25. 如果用户在审核界面修改了原始输入,那么修改后的结果是否会发送给 AI 呢
26. 右键状态栏图标,菜单项中 quit 前都那个删除图标删除掉不需要,
